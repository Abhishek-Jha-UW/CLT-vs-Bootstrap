# Copy this file to `.streamlit/secrets.toml` locally (do not commit real keys).
# Streamlit Community Cloud: paste the same keys in the Secrets UI.

OPENAI_API_KEY = "sk-REPLACE_ME"
# Optional: defaults to gpt-4o-mini in code if unset
OPENAI_MODEL = "gpt-4o-mini"
